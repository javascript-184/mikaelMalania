let day = "mondey";
if (day === "mondey" || day === "thusday")

switch(day) {
    case "mondey":
    case "thusday":
        console.log("Its a course day")
        break;
    default:
        console.log("Its not a course day");
}